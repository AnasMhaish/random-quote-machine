import React from 'react';

const MainLoader: React.FC = () => {
    return (
        <div className="main-loader">            
            <h1>Loading...</h1>
        </div>
    );
};

export default MainLoader;